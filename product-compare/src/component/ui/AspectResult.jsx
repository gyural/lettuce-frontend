import {React, useState} from 'react';
import styled, {css, keyframes} from "styled-components";


function AspectResult(props){
    const [Activation, setActivation] = useState(props.focus)
    props

}

export default AspectResult;